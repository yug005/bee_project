const express = require('express');
const router = express.Router();

// Auth routes consolidated in `routes/users.js`.
// This file kept as a placeholder for future auth endpoints.

module.exports = router;
